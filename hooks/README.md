# Hooks Placeholder

This directory is ready for any future hook scripts or assets the plugin needs.

`hooks.json` currently registers no hooks. Add real hook definitions there once you know the runtime events and commands you want to wire up.
